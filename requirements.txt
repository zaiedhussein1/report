# Base
# python>=3.8 # This should not be a pip requirement
pandas
numpy~=1.26.0 # For compatibility with torch and torchvision
scikit-learn
Pillow
opencv-python
matplotlib
transformers

# PyTorch (CPU specific versions)
torch==2.6.0
torchvision==0.21.0 # Compatible with torch 2.6.0

# NLP Evaluation
nltk
pycocoevalcap

# Optional for advanced features/logging
# tensorboard
# wandb
# chexpert-labeler (placeholder, will need specific package if available)
# GNN libraries (e.g., pyg)
